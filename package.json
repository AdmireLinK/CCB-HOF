{
  "devDependencies": {
    "vitepress": "2.0.0-alpha.15"
  },
  "scripts": {
    "dev": "vitepress dev hof",
    "build": "vitepress build hof && rm -rf dist && mv hof/.vitepress/dist dist",
    "preview": "vitepress preview hof"
  }
}